[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Official Next.js runtime — handles App Router, server components,
# route handlers, server actions, and middleware on Netlify.
[[plugins]]
  package = "@netlify/plugin-nextjs"
